# 1. Sentencias consecutivas mal formadas sin punto y coma
ENT -> 2
ENT - 3;

# 2. Estructura SI sin llave de apertura y con error interno en IMP
FUN probarSiSinLlave() {
    SI x > 0
        IMP ;
    IMP "Continuacion";
}

# 3. Múltiples errores en la misma línea (columnas distintas)
ENT a -> ; DEC b -> ;

# 4. Función con firma rota pero con errores dentro del cuerpo
FUN funcionFirmaRota(ENT a, ) {
    ENT y -> ;
    RET 0;
}
